PORT=4000
DB_FILE=./data/garachena.db

# Opcional: si se define, POST /api/products/import exige el header
# X-Admin-Key con este mismo valor. Sin definir, el endpoint queda abierto
# (solo para desarrollo/demo local).
# ADMIN_API_KEY=changeme
