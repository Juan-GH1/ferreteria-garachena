# Opcional. En desarrollo no se necesita: vite.config.js ya reenvía /api al
# backend en localhost:4000. Definir esto solo si el frontend se sirve desde
# un host distinto al backend (build de producción, otro entorno, etc.).
# VITE_API_BASE_URL=https://api.tu-dominio.cl/api
